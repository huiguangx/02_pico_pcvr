# 假数据测试 - 使用说明

## 🎯 用途

当后台还没实现震动功能时，用假数据测试前端震动是否正常工作。

---

## 🚀 使用方法

### 测试震动

打开 `Assets/Scripts/HapticMessageReceiver.cs`，找到第 50-59 行：

```csharp
// ============ 假数据测试 ============
// 取消下面的注释来测试震动功能（不调用真实服务器）
// 测试完成后重新注释掉即可
/*
string fakeJson = "{\"id\":\"vibrate\",\"data\":{\"side\":\"right\",\"intensity\":0.8,\"duration\":0.3}}";
Debug.Log("📨 [假数据测试] 收到消息: " + fakeJson);
ProcessMessage(fakeJson);
isPolling = false;
yield break;
*/
// ===================================
```

**取消注释（删除 `/*` 和 `*/`）：**

```csharp
// ============ 假数据测试 ============
// 取消下面的注释来测试震动功能（不调用真实服务器）
// 测试完成后重新注释掉即可

string fakeJson = "{\"id\":\"vibrate\",\"data\":{\"side\":\"right\",\"intensity\":0.8,\"duration\":0.3}}";
Debug.Log("📨 [假数据测试] 收到消息: " + fakeJson);
ProcessMessage(fakeJson);
isPolling = false;
yield break;

// ===================================
```

**运行游戏 → 自动触发震动！**

---

## 📝 修改参数测试

### 震动左手

```csharp
string fakeJson = "{\"id\":\"vibrate\",\"data\":{\"side\":\"left\",\"intensity\":0.5,\"duration\":0.2}}";
```

### 震动双手（强）

```csharp
string fakeJson = "{\"id\":\"vibrate\",\"data\":{\"side\":\"both\",\"intensity\":1.0,\"duration\":0.5}}";
```

### 轻微震动

```csharp
string fakeJson = "{\"id\":\"vibrate\",\"data\":{\"side\":\"right\",\"intensity\":0.3,\"duration\":0.05}}";
```

---

## ✅ 测试完成后

重新注释代码（加回 `/*` 和 `*/`），恢复真实服务器调用。

---

**就这么简单！注释/取消注释即可切换。** 🎮
